export interface Bpi {
        code: string,
        symbol: string,
        rate: number
        description: string,
        rate_float: number
}

export interface DbJson {
    id: number,
    title: string,
    score: number
}
